# Base URL of the Solinal Gestiona API (backend/).
# Optional: src/lib/api.ts already defaults to http://localhost:3001, which is
# the port backend/.env sets and which backend CORS_ORIGIN already allows for
# the Vite dev server — `npm run dev` works with no .env file at all.
VITE_API_URL=http://localhost:3001
